更新时间：2017-05-01
更新作者：hyt
更新内容：
--------1. 增加文件：netgate.py，查找所有可用串口，确认网关。
--------2. 更改文件：app.py，主程序只负责创建界面窗口和Canvas，不再检查串口（网关），网关检查交由UIstart.py创建NetGate实例。
--------3. 更改文件：UIstart.py，启动界面实例化网关检查类NetGate，确认网关，网关可用则显示开机动画，进行先一步操作，否则在启动界面显示“CAN 网关不可用...！”。
--------4. 更改文件：UItreat.py，治疗参数设置时，不输入或不输入点确认则为初始值，（更改前为空）。
--------5. 更改文件：UI*.py，界面实例调用start方法时发送主机状态。

